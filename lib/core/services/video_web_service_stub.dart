// stub mobile — pas de platformViewRegistry

class VideoWebService {
  static void registerPlayer(String viewId, String videoUrl) {
    // no-op sur Android/iOS — le widget VideoPlayerWeb (stub) est utilisé à la place
  }
}
